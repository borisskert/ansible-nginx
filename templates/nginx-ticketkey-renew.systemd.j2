[Unit]
Description = nginx ticketkey renew service

[Service]
ExecStart=/bin/bash -c "head -c 48 /dev/urandom > {{ticketkey_file_path}}"
ExecStopPost=-/bin/systemctl start {{reload_service_name}}
Restart = no
TimeoutSec = 60
