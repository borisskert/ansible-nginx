image_name: nginx
image_version: 1.17.9-alpine

https_port: 443
http_port: 80

configs: {}

volume: ''
conf_folder: "{{volume}}/conf.d"
rules_folder: "{{volume}}/rules"
certs_folder: "{{volume}}/certs"
ssl_folder: "{{volume}}/ssl"
www_folder: "{{volume}}/www"

working_directory: /opt/nginx
